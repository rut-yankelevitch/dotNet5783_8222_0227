<Window x:Class="PL.Order.OrderTrackingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Order"
        mc:Ignorable="d"
        DataContext="{Binding TrackingOrder, RelativeSource={RelativeSource Mode=Self} }"
        
        Title="OrderTrackingWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding ID}" Grid.Row="0" FontSize="20" />
        <TextBlock Text="{Binding Status}" Grid.Row="1" FontSize="20"/>
        <ListView ItemsSource="{Binding Tuples}"  Grid.Row="2" FontSize="20"/>
        <Button  Content="תצוגת הזמנה" Width="200"  Margin="0,0,0,0" Click="showOrder_Click"/>
    </Grid>
</Window>
